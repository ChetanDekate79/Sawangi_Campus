import React from "react";
import "./home.css"; // Import the CSS file for the Home component

const Home = () => {
  return (
    <div className="home-container">
      <img src="home.png" className="image-container" title="Page"></img>
    </div>
  );
};

export default Home;
